# Crypto Queue Screener

Static market-map style screener for Crypto Signal Observer queue/watchlist.

Source data is exported from local paper observer state:

```text
data/m5-flag-observer-state.json
```

The page shows current watchlist symbols, active setups and open paper trades for the M5 flag system.
